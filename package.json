{
  "name": "who",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "yarn build:zig && parcel index.html",
    "build:zig": "zig build-lib add.zig -target wasm32-freestanding",
    "build": "yarn build:zig && parcel build index.html"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  }
}
